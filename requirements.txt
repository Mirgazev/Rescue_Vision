# =====================================================================
# Rescue Vision — Python зависимости
# =====================================================================
# Версии соответствуют аппаратно-программной конфигурации, на которой
# выполнены все эксперименты ВКР (см. Таблица 5 раздела 4.1).
#
# Установка:
#   python3 -m pip install -r requirements.txt
#
# Дополнительно требуются системные ROS 2 Foxy-пакеты, устанавливаемые
# через apt: см. раздел «Установка» в docs/installation.md.
# =====================================================================

# ----- Базовый стек инференса -----
torch==2.10.0
torchvision==0.25.0
ultralytics==8.4.14

# ----- Компьютерное зрение -----
opencv-python==4.13.0.92
opencv-contrib-python==4.13.0.92
numpy>=1.26,<2.0
Pillow>=10.0

# ----- Обработка изображений и работа с датасетами -----
scipy>=1.11
scikit-image>=0.22
PyYAML>=6.0

# ----- Прогресс-бары, логи, утилиты -----
tqdm>=4.66
psutil>=5.9
matplotlib>=3.8
pandas>=2.0
tabulate>=0.9

# ----- Метрики -----
scikit-learn>=1.3.0
pycocotools>=2.0.7

# ----- Метрики трекинга (MOT17: IDF1, MOTA) -----
motmetrics>=1.4.0
lap>=0.4.0
filterpy>=1.4.5

# ----- RealSense (только при работе с реальной камерой D435i) -----
# При запуске на машине без камеры можно закомментировать эту строку.
pyrealsense2>=2.55.0

# =====================================================================
# Примечание о ROS 2-зависимостях
# =====================================================================
# Следующие пакеты ставятся не через pip, а через системный apt и
# собственный colcon-workspace:
#
#   ros-foxy-rclpy
#   ros-foxy-sensor-msgs
#   ros-foxy-vision-msgs
#   ros-foxy-diagnostic-msgs
#   ros-foxy-cv-bridge
#   ros-foxy-realsense2-camera
#   ros-foxy-rosbag2
#   ros-foxy-launch-ros
#
# Сам пакет сообщений rescue_vision_msgs собирается из исходников
# командой `colcon build --packages-select rescue_vision_msgs`.
